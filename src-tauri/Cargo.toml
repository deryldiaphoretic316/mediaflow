[package]
name = "mediaflow-tauri"
version = "0.1.0"
description = "MediaFlow desktop shell (Tauri + FastAPI)"
authors = ["Valerii Korobeinikov"]
license = "MIT"
repository = "https://github.com/vkgeorgia/mediaflow"
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
